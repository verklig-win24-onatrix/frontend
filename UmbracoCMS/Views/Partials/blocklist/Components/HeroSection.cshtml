@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Umbraco.Cms.Core.Models.Blocks.BlockListItem>;
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;
@{
  var content = (ContentModels.HeroSection)Model.Content;
}

<section class="hero-section">

	<div class="bg-image">
		@if (content.HeroBgImage != null)
		{
			<img src="@content.HeroBgImage.Url()" />
		}
	</div>

	<div class="wrapper">
		<div class="container">
			<div class="flex">
				<h1>@content.HeroTitle</h1>
				<div class="rte">@Html.Raw(content.HeroText)</div>

				@if (content.HeroLink != null)
				{
					<a href="@content.HeroLink.Url">@content.HeroLink.Name</a>	
				}
			</div>
		</div>
	</div>

</section>